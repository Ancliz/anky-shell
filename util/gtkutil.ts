import { Gtk } from "ags/gtk4";

export const { VERTICAL, HORIZONTAL } = Gtk.Orientation;
export const { START, CENTER, END, FILL } = Gtk.Align;
export const { SLIDE_RIGHT, SLIDE_DOWN } = Gtk.RevealerTransitionType;